/** Placeholder. The four items and the positioning arrive in M5. */
export default function WidgetMenu() {
  return (
    <div className="h-full w-full rounded-xl border border-border bg-background p-2 backdrop-blur-xl">
      <p className="px-2 py-1 font-sans text-sm text-muted-foreground">Menu</p>
    </div>
  );
}
